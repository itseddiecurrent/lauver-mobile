# Copy to .env.staging and set the external connection URL for lauver_staging.
# This file is loaded only by the Step 06 / Step 07 / Step 08 acceptance verifiers.
STAGING_DATABASE_URL=postgresql://replace-user:replace-password@replace-staging-host/lauver_staging?sslmode=require
