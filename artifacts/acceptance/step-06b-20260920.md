# Step 06B acceptance — 2026-09-20

## Environment

- App: `Lauver-Staging`
- API: `https://lauver-api-staging.onrender.com`
- Device: `Edward的iPhone`, iPhone 17e, iOS 26.6.1
- Test accounts: injected from the local `.env` into the UI-test process only; no credentials are recorded here.

## Render + real-device evidence

The targeted `LauverUITests.testLiveMatchAndConversationOnDevice()` was executed against the deployed Render staging API on the connected iPhone 17e.

Passed flow:

1. Account 1 logged in and opened the existing mutual Match.
2. Account 1 opened the conversation and sent a unique message.
3. Account 2 logged in and saw the latest Stream message in the Match row.
4. Account 2 saw the unread badge before opening the conversation.
5. Account 2 opened the conversation; returning to Match cleared the unread badge.

Result bundle: `/tmp/lauver-step06b-live-8.xcresult`

`xcresulttool` summary: 1 test, 1 passed, 0 failed.

## Fixes made during acceptance

- Match rows now load Stream's latest message and unread count.
- Opening a direct conversation marks the channel read and immediately updates the Match row, even before Stream's asynchronous local state update arrives.
- Match ordering is stable by `matchedAt` descending.
- Fixed candidate photo accessibility text interpolation and Match-row More button identifiers.

## Step 06B accessibility follow-up

- The user-provided nine-photo upload/reorder/primary/deletion/preview manual check is supplemented by the connected-device ninth-photo boundary check below.
- With VoiceOver already enabled on the connected iPhone 17e, the real-device accessibility UI checks passed: Match, Start matching, Like, Pass, View Profile, all five tabs, and Match Filters were exposed with the expected labels and identifiers.
- Result bundle: `/tmp/lauver-step06b-voiceover.xcresult`; 2 tests, 2 passed, 0 failed. The test runner used direct accessibility-element actions rather than VoiceOver rotor gestures.

## Photo limit evidence

- Render staging account 2 has 9 published photos.
- On the connected iPhone 17e, Edit Profile shows `Photo limit reached` and does not offer another upload slot.
- Backend route test sends 10 photo upload requests and receives HTTP 422 without calling the upload-ticket service.
- Backend result: `backend/tests/profile-routes.test.ts`, 8/8 passed.
- Device result bundle: `/tmp/lauver-step06b-photo-limit-11.xcresult`, 1/1 passed.

## Accessibility and appearance evidence

- Large Dynamic Type + dark appearance: `/tmp/lauver-step06b-large-dark.xcresult`, 1/1 passed.
- Small Dynamic Type + light appearance: `/tmp/lauver-step06b-small-light.xcresult`, 1/1 passed.
- Both were executed on `Edward的iPhone` (iPhone 17e, iOS 26.6.1).
