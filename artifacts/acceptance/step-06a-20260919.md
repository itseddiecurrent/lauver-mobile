# Step 06A Acceptance — Match API

## Automated and local API acceptance

- Backend unit tests: **180/180 passed**.
- PostgreSQL migration-from-zero integration tests: **69/69 passed**.
- The expanded Step 06 acceptance verifier ran against the local `lauver_test` database and local API: **96 checks passed**.
- The verifier covered default opt-out, candidate browsing and Like rejection while opted out, opt-in persistence, candidate visibility, one-sided Like, mutual Like, unique Match creation, repeated Like idempotency, Unmatch, prevention of restoring an unmatched pair, bidirectional Block isolation, the 15 Like UTC quota boundary, and the existing Discover filtering/pagination/privacy checks.
- The verifier created 34 disposable accounts and deleted all 34 accounts and dependent rows. No fixture journal remained.

## Staging blocker

- The same verifier was attempted against Render staging on 2026-09-19.
- It stopped while connecting to the external `lauver_staging` PostgreSQL database because the current client IP is not allowed by the database network allowlist.
- No staging accounts or database rows were created by the failed attempt.
- The staging run should be repeated after the external database allowlist permits this machine.

## Code changes

- Added Match checks to `backend/scripts/verify-step-06-staging.ts`.
- Added the real Match service to the Step 06 integration acceptance harness.
- Fixed repeated Like handling so an already-unmatched pair returns `409 match_unmatched` instead of being incorrectly treated as an idempotent success.
